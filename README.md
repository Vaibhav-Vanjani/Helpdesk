Flow of helpdesk:

1) admin creates a project/organisation and assigns project to manager(existing/Create new manager invitation ID)
2) manager exposes project to a client and assign bug updates to assignees
3) client raises a bug
4) assignees gets bug
5) assignees test across different stages
6) client and manager gets notified